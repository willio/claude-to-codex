# .c2cignore — gitignore syntax; these paths become invisible to Claude.
# Sensitive defaults (.env, keys, credentials, SSH…) are always enforced
# even without this file.

# internal design docs
internal-notes/

# large generated fixtures
fixtures/**/*.snap
